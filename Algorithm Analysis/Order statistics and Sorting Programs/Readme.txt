Order Statistics and Sorting:

Specification: 
Import project diretory into JetBrains PyCharm Community Edition 4.5.3.

Generate Input files:
You can generate new input files using Randomnumbergenerator.py
Change the list size and for loop condition.
We have generated few sample input files.
Input files generated have average case values which we have used to plot graphs. So, the graphs are also average case graphs.

Run:
Click on Run button in PyCharm IDE to run .py python files.